# ─────────────────────────────────────────────────────────────────────────────
# Canonical production nginx site config is:
#   deploy/nginx-sgc-erp.conf
#
# Apply on the Ubuntu server (after git pull):
#   sudo bash scripts/nginx-production-setup.sh
#
# Do not duplicate server_name (tovus.net / www / IP) across default + sgc-erp —
# that causes nginx warnings and unpredictable vhost choice.
# ─────────────────────────────────────────────────────────────────────────────
