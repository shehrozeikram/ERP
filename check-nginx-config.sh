#!/usr/bin/env bash
# On server: sudo nginx -t && sudo ls -la /etc/nginx/sites-enabled/
set -euo pipefail
echo "Run on server: sudo nginx -t && sudo bash scripts/check-nginx-sites.sh (from repo) or inspect /etc/nginx/sites-enabled/"
