# Server Configuration
PORT=5001

# Test credentials (for rental bulk invoice test - tests/rental-bulk-invoice-jan2026.test.js)
TEST_EMAIL=your_test_email@example.com
TEST_PASSWORD=your_test_password
NODE_ENV=development

# MongoDB Configuration
# Production (server): set only MONGODB_URI on the production server - app uses this when NODE_ENV=production
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/sgc_erp?retryWrites=true&w=majority

# Local development: set MONGODB_URI_LOCAL to use a separate local DB; production DB remains untouched
# When NODE_ENV is not "production" and MONGODB_URI_LOCAL is set, the app connects to local MongoDB only
# Install MongoDB locally or use Docker, then set for example:
# MONGODB_URI_LOCAL=mongodb://localhost:27017/sgc_erp_local
# MONGODB_URI_LOCAL=

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=24h

# Email Configuration (Optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_password

# File Upload Configuration
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# LinkedIn API Configuration (Optional - for job posting to LinkedIn)
LINKEDIN_ACCESS_TOKEN=your_linkedin_access_token_here
LINKEDIN_ORGANIZATION_ID=your_linkedin_organization_id_here

# Frontend URL (for application links)
FRONTEND_URL=http://localhost:3000

# Email Configuration (for shortlist notifications)
SMTP_HOST=mail.sgc.international
SMTP_PORT=465
SMTP_USER=hr@sgc.international
SMTP_PASS=@Pakistan333
EMAIL_PASSWORD=your_email_password_here

# Attendance System Configuration
# ZKTeco Device Settings (legacy)
ZKTECO_HOST=45.115.86.139
ZKTECO_PORT=85
ZKTECO_WEBSOCKET_URL=ws://45.115.86.139:85/base/dashboard/realtime_punch/
ZKTECO_TIMEOUT=10000
ZKTECO_MSG_DELAY=4000

# ZKBio Time System Settings (new)
ZKBIO_BASE_URL=http://45.115.86.139:85
ZKBIO_USERNAME=superuser
ZKBIO_PASSWORD=SGCit123456
ZKBIO_TIMEOUT=10000 